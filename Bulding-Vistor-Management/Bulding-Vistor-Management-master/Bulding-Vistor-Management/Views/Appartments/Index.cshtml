@model IEnumerable<Bulding_Vistor_Management.Models.Appartment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AppartmentNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AppartmentNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AppartmentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AppartmentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AppartmentId })
        </td>
    </tr>
}

</table>
